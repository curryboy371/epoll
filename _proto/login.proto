syntax = "proto3";

package login;

import "common.proto";

message LoginRequest {
  string id = 1;
  string password = 2;
}

message LoginResponse {
  bool success = 1;
  chat_common.User sender = 2;
  repeated chat_common.User users = 3;
}